DO
    {%for param in namelist%}
        {%include 'value_initialization.txt'%}
    {% endfor %}
    READ (lun, NML=bac, END=100, IOMSG=msg, ERR=900)
    IF (TRIM(species).EQ.TRIM(grp%name)) EXIT
END DO

{% for param in namelist %}
    {% if param.type == 'bool' %}
       {{param.name.text}} = .FALSE
    {% else %}
    grp%{{param.name.text}} = {{param.name.text}}
    {% endif %}
{% endfor %}